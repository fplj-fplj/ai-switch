{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "android",
  "description": "Permissions for the Android app. The panel is the same React bundle as the desktop window and reaches the Rust side over IPC, but the only plugin compiled into this build is Tauri's own core, so core:default is the whole list.",
  "platforms": ["android"],
  "windows": ["main"],
  "permissions": ["core:default"]
}
